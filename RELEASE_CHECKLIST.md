# HodoQ リリース確認

## 自動確認

- [ ] `cargo fmt --check`
- [ ] `cargo clippy --all-targets --all-features -- -D warnings`
- [ ] `cargo test --all-targets --all-features`
- [ ] `cargo test --release --lib --all-features performance_ -- --ignored --test-threads=1 --nocapture`（性能テスト2件が実行されること）
- [ ] Windows／Apple Silicon macOSのCIで`cargo build --release`

## GUI確認（Windows 11／Apple Silicon macOS）

- [ ] clone後、READMEの手順だけでビルド・直接起動できる
- [ ] DBと設定がclone先の`.hodoq/`へ保存され、`target/release`からの起動でも同じ場所を使用する
- [ ] 中央編集カードの「変更を保存」が成功するとカードが閉じ、入力エラー時は開いたままになる
- [ ] 「その他」から絞り込み、複数選択、データ管理、取り消しを開ける
- [ ] サイドバー境界をドラッグして幅を変更でき、再起動後も保持される
- [ ] 日本語IMEでタイトルとメモを入力できる
- [ ] タスク、保存済みビューを作成・編集・削除できる
- [ ] プロジェクト・タグの操作が表示されず、既存の分類データは保持される
- [ ] 日付なし／日付のみ／日時指定の納期が再起動後も保持される
- [ ] 納期欄のクリックでカレンダーが開き、開いたまま直接入力・カーソル移動・日付選択ができる
- [ ] 時刻候補をパネル内で選択でき、分単位の直接入力・時刻の解除・未定への変更ができる
- [ ] Enterで納期を確定し、Esc・外側クリックでは入力を消さずパネルだけ閉じる
- [ ] 狭い・短い画面で納期パネルが入力欄に重ならず、すべての操作へスクロールできる
- [ ] 検索・絞り込み・並び替えが3表示で一致する
- [ ] 複数選択したタスクを一括更新できる
- [ ] Shift範囲選択、Ctrl／Cmd個別選択、全選択が動作する
- [ ] 高優先度・グループ化・絞り込みのあるリストでも、矢印キー移動とShift範囲選択が表示順と一致し、一括更新が選択範囲だけに反映される
- [ ] リストの手動順とボードの状態をドラッグ操作で変更できる
- [ ] 行の右クリックメニューと「操作を検索」から各操作を実行できる
- [ ] タスク編集が右側に表示され、短い・狭いウィンドウで下端までスクロールしても保存・アーカイブ・ゴミ箱ボタンの間隔と表示が保たれる
- [ ] カレンダーで今日・土日・優先度・納期未定領域を判別できる
- [ ] タイトルとメモが入力停止後に自動保存され、終了直前の入力も保持される
- [ ] 手動バックアップ、復元、CSV／JSON出力がGUIから完了する
- [ ] ゴミ箱の復元と、確認付きの完全削除が動作する
- [ ] 同じデータディレクトリで二重起動できない
- [ ] ウィンドウの描画、スクロール、終了が正常である
- [ ] 書き込み不能時に読み取り専用表示となり、「DBを再試行」が動作する
