# AGENTS.md

## General policy

- 日本語で簡潔かつ丁寧に回答してください。
- このリポジトリは、実装・テスト・ドキュメントを含め、AIによる開発を前提とします。
- 依頼された作業の範囲で、ブランチ作成、署名付きコミット、push、PR作成、Codexへのレビュー依頼、指摘への返信・修正・再レビュー、条件を満たしたPRのマージまで進めて構いません。各段階で改めてユーザーの許可を求める必要はありません。
- ユーザーが作業範囲や停止位置を明示した場合は、その指示を優先してください。

## Commit signing policy

- コミットは署名付きで作成してください。
- コミットを作成・再作成する操作（commit、amend、rebase、cherry-pick、mergeなど）の前に、`commit.gpgsign`、`user.signingkey`、署名方式と鍵の利用可否を確認してください。
- 自動署名が無効でも、`git commit -S`などで署名を明示してください。署名できない場合に、未署名コミットへ切り替えてはいけません。
- 作成後は`git verify-commit HEAD`などで署名を検証してください。GitHub上で生成されるマージコミットも署名が付く方法を選び、マージ後に検証してください。

## PR・レビュー・マージの手順

1. `main`への直接pushは行わず、原則として`codex/`で始まる作業ブランチを使用してください。
2. 実装と必要な検証を完了し、署名付きコミットをpushしてPRを作成してください。PR本文には、問題と修正後の挙動、検証結果、関連Issue、未確認事項を記載してください。
3. PRのコメントに`@codex review`を投稿してください。レビュー依頼時のコミットSHAを記録し、どの版へのレビューか確認できるようにしてください。
4. Codexのレビュー完了を待ち、レビュー本文・インラインコメント・会話コメントを確認してください。無応答、受付を示す👀、利用制限やエラーは「指摘なし」ではありません。
5. 指摘があれば内容を検証し、修正・テストを行い、署名付きコミットで同じPRを更新してください。各指摘には対応内容と検証結果を返信してください。修正不要と判断した場合も根拠を返信し、自己判断で指摘を無視してマージしてはいけません。
6. 修正をpushしたら、再び`@codex review`で最新のコミットへのレビューを依頼してください。レビュー後にコード・テスト・ドキュメントの変更や競合解消を行った場合も同様です。最新の変更に対する指摘がなくなり、以前の指摘も解決するまで、このサイクルを繰り返してください。
7. 次の条件をすべて満たした時点で、追加のユーザー確認なしにマージしてください。
   - 最新のPR先頭コミットに対するCodexのレビューが完了し、指摘なしの結果を確認できること。👍による完了通知の場合も、対象のレビュー依頼・コミットとの対応を確認すること。
   - 以前のレビューを含め、未解決の指摘や変更要求が残っていないこと。
   - 最新の変更に対するWindows/macOSのCIと、必要なローカル検証が成功していること。
   - 競合がなく、リポジトリの保護ルールを満たしていること。管理者権限などで検証やレビューを迂回しないこと。
8. マージ直前にPR先頭のSHAを再確認し、レビュー済みのSHAと一致することを確認してください。マージ操作にも期待するSHAを指定し、未レビューの更新を取り込まないようにしてください。
9. マージ後は結果とコミット署名を確認し、関連Issueのチェックリストを実際の対応状況に合わせて更新してください。未対応項目が残るIssueは閉じず、PR・検証結果・残件をユーザーへ簡潔に報告してください。

レビューを実行できない、結果を確認できない、CIが失敗しているなど、条件を満たせない場合はマージせず、阻害要因と現在の状態を報告してください。

レビュー依頼方法の参照: [OpenAI公式ドキュメント](https://developers.openai.com/ja-JP/docs/third-party/github)

## 検証

- `README.md`、`docs/DESIGN.md`、`.github/workflows/ci.yml`の検証手順に従ってください。
- 不具合修正では、可能な限り修正前に失敗し修正後に成功する回帰テストを追加してください。
- GUIの自動テスト成功と実機確認は区別し、未実施の確認を実施済みと記載しないでください。
